<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.github.kaguya.dao.mapper.DocumentGroupMapper">

    <select id="getDocsParent" resultType="com.github.kaguya.model.DocumentGroup">
      SELECT
        document_id, parent_id, order_id, title
      FROM document_group
      WHERE
        category_id = #{categoryId}
        AND parent_id = #{ROOT}
      ORDER BY order_id
    </select>

    <select id="getDocsGroup" resultType="com.github.kaguya.model.DocumentGroup">
      SELECT
        document_id, parent_id, order_id, title
      FROM document_group
      WHERE
        parent_id = #{documentId}
      ORDER BY order_id
    </select>
</mapper>